import {data} from './data';

// renders 1 product, use in current collection to render a grid. 
export const Product = () => {
  return(
    <h1>
      This will be the card generator for each product. Details to fill in each card come from products/details
    </h1>
  )
}